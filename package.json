{
  "name": "command-robot-base",
  "version": "1.0.0",
  "description": "Command robot base project with TypeScript configuration",
  "workspaces": [
    "ThriftTsConfig"
  ],
  "scripts": {
    "config": "npm run -w ThriftTsConfig config --",
    "generate-thrift": "npm run -w ThriftTsConfig generate-thrift --"
  }
}
